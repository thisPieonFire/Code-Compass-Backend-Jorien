spring:
  datasource:
    url: jdbc:postgresql://db.wlyfeciabevxeodttvye.supabase.co:5432/postgres
    username: postgres
    password: Createment
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update   # options: none, validate, update, create, create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect


